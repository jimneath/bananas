<h1>Listing bananas</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Colour</th>
    <th>Length</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bananas.each do |banana| %>
  <tr>
    <td><%= banana.name %></td>
    <td><%= banana.colour %></td>
    <td><%= banana.length %></td>
    <td><%= link_to 'Show', banana %></td>
    <td><%= link_to 'Edit', edit_banana_path(banana) %></td>
    <td><%= link_to 'Destroy', banana, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Banana', new_banana_path %>
