# see the following documention for more details:
# https://support.cloud.engineyard.com/entries/21092002-customize-trinidad-and-jvm-options#topic2

# add_java_option helper function
# a function for adding options to the JAVA_OPTS environment variable.
# 
# eg. add_java_option -XX:UnlockExperimentalVMOptions -XX:+UseG1GC
 
# add_jruby_option helper function
# a helper function for adding options to the JRUBY_OPTS environment variable.
# 
# eg. add_jruby_option -Xthread.pool.enabled=true
 
# set_jvm_memory helper function
# use this function to set the JVM maximum heap size if the default value is not sufficient. The heap 
# size is set dynamically depending on the size of your instance; find the default value in 
# /data/<%= @app_name %>/shared/config/env.
# 
# eg. set_jvm_memory 2G 

# to add other environment variables, specify them directly and export.
# 
# eg. export CLASSPATH=/data/app_name/current/lib/iText.jar 